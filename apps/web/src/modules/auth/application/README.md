# Camada `application` (auth)

Contém casos de uso e serviços de aplicação relacionados à autenticação.

## Exemplo

```ts
// loginUser.ts
export async function loginUser(email: string, password: string) {
  // lógica de autenticação
}
``` 