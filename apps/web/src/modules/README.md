# Pasta `modules`

Contém os subdomínios do sistema, cada um isolado em seu próprio módulo seguindo DDD + Clean Architecture.

## Exemplo

```
modules/
  auth/
  users/
```

Cada módulo possui as camadas: `domain`, `application`, `infrastructure` e `presentation`. 