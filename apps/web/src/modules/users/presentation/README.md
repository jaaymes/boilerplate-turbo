# Camada `presentation` (users)

Contém componentes de UI, controllers e rotas relacionados a usuários.

## Exemplo

```tsx
// UserList.tsx
export function UserList() {
  return <ul>{/* lista de usuários */}</ul>;
}
``` 