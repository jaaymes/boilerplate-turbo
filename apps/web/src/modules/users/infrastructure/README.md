# Camada `infrastructure` (users)

Contém implementações concretas de integrações externas, como APIs ou banco de dados, para usuários.

## Exemplo

```ts
// UserRepository.ts
export async function fetchUsers() {
  // chamada à API de usuários
}
``` 