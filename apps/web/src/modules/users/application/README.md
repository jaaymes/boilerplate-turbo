# Camada `application` (users)

Contém casos de uso e serviços de aplicação relacionados a usuários.

## Exemplo

```ts
// createUser.ts
export async function createUser(name: string, email: string) {
  // lógica de criação de usuário
}
``` 